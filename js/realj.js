const accountLink = document.querySelector('.links-container a:first-child');
const passwordLink = document.querySelector('.links-container a:last-child');

accountLink.addEventListener('click', () => {
  // navigate to account page
});

passwordLink.addEventListener('click', () => {
  // navigate to password reset page
});
